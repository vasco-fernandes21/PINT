import React from 'react';
import EventosList from '../../components/eventos/eventosList';

const Eventos = () => {
    return (
        <div>
            <EventosList />
        </div>
    );
};

export default Eventos;