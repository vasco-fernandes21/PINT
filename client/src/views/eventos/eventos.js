import React from 'react';
import EventoList from '../../components/eventos/eventoList';

const Eventos = () => {
    return (
        <div>
            <EventoList />
        </div>
    );
};

export default Eventos;